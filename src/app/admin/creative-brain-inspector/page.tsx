import { BrainInspector } from "./brain-inspector";

export const metadata = {
  title: "Creative Brain Inspector — Admin",
};

export default function CreativeBrainInspectorPage() {
  return <BrainInspector />;
}
